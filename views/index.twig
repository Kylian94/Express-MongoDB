{% extends 'layout.twig' %}

{% block body %}
	<div class="d-flex justify-content-center">
		<h1>{{title}}</h1>
	</div>
	<div class="d-flex justify-content-center">
		<div class="col-4 d-flex flex-column p-3 bg-light">
			<h2 class="text-center mb-3">Welcome to MongoAuth</h2>
			<div class="d-flex justify-content-center align-items-center flex-column">
				<a href="/user/signin" class="btn btn-primary my-3">Log in</a>
				<a href="/user/register" class="btn btn-primary my-3">Register</a>
				<a href="/user/account" class="btn btn-primary my-3">Account</a>
			</div>
		</div>
	</div>
{% endblock %}
